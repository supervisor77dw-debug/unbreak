# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# UNBREAK ONE - LOCAL DEVELOPMENT ENV
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Kopiere diese Datei nach .env.local und fülle die Werte aus.
# NIEMALS .env.local in Git committen!
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ===================================
# STRIPE CONFIGURATION
# ===================================

# PFLICHT: Checkout-Modus (test|live)
# Für lokale Entwicklung immer "test"
STRIPE_CHECKOUT_MODE=test

# PFLICHT: Stripe API Keys
# Von: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY_TEST=your_test_key_here
STRIPE_SECRET_KEY_LIVE=your_live_key_here

# PFLICHT: Webhook Secrets (pipe-separiert für mehrere)
# Für lokale Entwicklung: stripe listen --forward-to localhost:3000/api/webhooks/stripe
# Der Output zeigt: "Ready! Your webhook signing secret is whsec_..."
STRIPE_WEBHOOK_SECRETS=your_webhook_secret_here

# ===================================
# EMAIL CONFIGURATION (Resend)
# ===================================

# PFLICHT: Resend API Key
# Von: https://resend.com/api-keys
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# PFLICHT: Absender-Adressen
# MUSS eine verifizierte Domain in Resend sein
EMAIL_FROM_ORDERS=orders@unbreak-one.com
EMAIL_FROM_SUPPORT=support@unbreak-one.com
EMAIL_FROM_NO_REPLY=no-reply@unbreak-one.com

# PFLICHT: Admin-Email für Bestellbenachrichtigungen
ADMIN_ORDER_EMAIL=admin@example.com

# ===================================
# SUPABASE DATABASE
# ===================================

# PFLICHT: Supabase URL
# Von: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://XXXXXXXXXXXX.supabase.co
SUPABASE_URL=https://XXXXXXXXXXXX.supabase.co

# PFLICHT: Supabase Keys
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.XXXXXX
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.XXXXXX

# ===================================
# NEXTAUTH AUTHENTICATION
# ===================================

# PFLICHT: NextAuth Secret (generiere mit: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-here-minimum-32-characters

# PFLICHT: NextAuth URL (für lokale Entwicklung)
NEXTAUTH_URL=http://localhost:3000

# ===================================
# ADMIN API (Optional für lokalen Test)
# ===================================

# Admin API Key für lokalen Test
ADMIN_API_KEY=local-test-key-for-admin-api

# ===================================
# SITE URLS
# ===================================

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_CONFIGURATOR_DOMAIN=https://unbreak-3-d-konfigurator.vercel.app

# ===================================
# PRISMA DATABASE (Neon PostgreSQL)
# ===================================

# Von: Neon Console → Connection Details
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LOKALER TESTFLOW
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1. Kopiere Werte aus Vercel Dashboard → Settings → Environment Variables
#
# 2. Terminal 1: Starte Dev-Server
#    npm run dev
#
# 3. Terminal 2: Starte Stripe CLI
#    stripe login
#    stripe listen --forward-to localhost:3000/api/webhooks/stripe
#    → Kopiere das angezeigte whsec_... nach STRIPE_WEBHOOK_SECRETS
#
# 4. Shop testen:
#    - http://localhost:3000/shop
#    - Produkt in Warenkorb
#    - Checkout
#    - Testkarte: 4242 4242 4242 4242, beliebiges Datum, beliebige CVC
#
# 5. Prüfen:
#    - Order in simple_orders (Supabase Dashboard)
#    - Status: paid
#    - Emails in Resend Dashboard
#    - Admin Panel: http://localhost:3000/admin/orders
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
