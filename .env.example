# Environment Variables Template
# Copy to .env.local and fill in your values

# =====================================================
# SUPABASE
# =====================================================
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api

SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =====================================================
# STRIPE
# =====================================================
# Get these from: https://dashboard.stripe.com/test/apikeys

# Test Mode (for development)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_webhook_secret_here

# Live Mode (for production - use separate keys!)
# STRIPE_SECRET_KEY=your_live_stripe_key_here
# STRIPE_WEBHOOK_SECRET=your_live_webhook_secret_here

# NOTE: No Price IDs or Shipping Rate IDs needed!
# System uses DB-driven pricing (Adminpanel) with dynamic price_data
# Stripe receives calculated amounts only, no pre-configured Products/Prices

# =====================================================
# APPLICATION
# =====================================================
# Your website URL (no trailing slash)
# IMPORTANT: Use main domain only, NO subdomains
# All routes (/shop, /login, /ops, /admin) use path-based routing

# Development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Production (main domain only - no shop.unbreak-one.com or op.unbreak-one.com)
# NEXT_PUBLIC_BASE_URL=https://unbreak-one.com

# =====================================================
# CONFIGURATOR (NEW - Redirect Flow)
# =====================================================
# External 3D Configurator URL (standalone app)
NEXT_PUBLIC_CONFIGURATOR_DOMAIN=https://unbreak-3-d-konfigurator.vercel.app

# =====================================================
# OPTIONAL (Phase 2)
# =====================================================

# =============================================
# EMAIL CONFIGURATION (Resend)
# =============================================
# Resend API Key (get from https://resend.com/api-keys)
# RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Email Kill-Switch (CRITICAL - prevents 403 errors on Free Plan)
# EMAILS_ENABLED=false   # Preview mode: Logs emails, doesn't send (DEFAULT)
# EMAILS_ENABLED=true    # Send mode: Actually sends via Resend API (only after domain verification!)

# Email FROM addresses (auto-selected by email type)
# EMAIL_FROM_ORDERS=orders@your-domain.com
# EMAIL_FROM_SUPPORT=support@your-domain.com
# EMAIL_FROM_NO_REPLY=no-reply@your-domain.com

# Legacy fallback FROM (if specific addresses not set)
# RESEND_FROM=no-reply@your-domain.com

# Optional: Shop owner notifications (deprecated)
# SHOP_OWNER_EMAIL=owner@your-domain.com

# Email (SendGrid - alternative, not used)
# SENDGRID_API_KEY=SG.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Monitoring (Sentry)
# SENTRY_DSN=https://XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX@sentry.io/XXXXXXX

# =====================================================
# SECURITY NOTES
# =====================================================
# 
# 1. NEVER commit .env.local to git
# 2. Add .env.local to .gitignore
# 3. Use separate Stripe keys for test/live
# 4. Rotate keys if compromised
# 5. Use service_role key ONLY server-side
# 
# =====================================================
